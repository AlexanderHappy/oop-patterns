Реализация паттерна Стратегия позволяет создавать новые алгоритмы для какой-то сущности,
в моем случае это способы оплаты и уведомления.
НО!
Что делать если мне необходимо расширить какой-то класс, то есть докинуть новые методы, когда
интерфейс уже есть и создан?


if (!empty($attributes)) {
    $attribute = $attributes[0]->newInstance();
    $strategies[$className] = $attribute->method;
}

Что делать если атрибутов станет несколько? Это уже не будет работать $attributes[0].
